import styled from "styled-components";

export const VideosSectionStyles = styled.section`
  padding: 50px;
  display: grid;
  grid-template-columns: repeat(auto-fill, 256px);
  justify-content: center;
  gap: 50px;
`;
